<?php

class ServicereportsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('servicereports')->truncate();

		$servicereports = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('servicereports')->insert($servicereports);
	}

}
